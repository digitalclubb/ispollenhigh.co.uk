<!--
	The +page.server.ts above always redirects, so this template never renders.
	A minimal stub keeps SvelteKit happy and serves as a fallback if a future
	error path lands here.
-->
<p>Looking that up.</p>
