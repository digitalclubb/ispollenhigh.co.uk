<script lang="ts">
	// Phase 0 placeholder. Phase 1 replaces this with the live answer.
</script>

<section class="hero">
	<p class="kicker">ispollenhigh.co.uk</p>
	<h1>Coming soon.</h1>
	<p class="lede">
		A straight answer to the question hayfever sufferers actually ask. Is pollen high in your area today?
	</p>
</section>

<style>
	.hero {
		padding-block: var(--sp-9) var(--sp-7);
	}

	.kicker {
		font-size: var(--fs-sm);
		font-weight: var(--weight-medium);
		letter-spacing: var(--tracking-loose);
		text-transform: uppercase;
		color: var(--ink-mute);
		margin-bottom: var(--sp-5);
	}

	h1 {
		font-family: var(--font-display);
		font-size: var(--fs-display);
		font-weight: var(--weight-display);
		line-height: var(--lh-tight);
		letter-spacing: var(--tracking-tight);
		color: var(--ink);
		max-width: 14ch;
	}

	.lede {
		margin-top: var(--sp-5);
		font-size: var(--fs-md);
		color: var(--ink-soft);
		max-width: 48ch;
	}
</style>
